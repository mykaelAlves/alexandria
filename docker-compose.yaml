services:
  test-db:
    image: postgres:15-alpine
    container_name: alexandria_db_test
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: alexandriadb
    ports:
      - "5432:5432"
    tmpfs:
      - /var/lib/postgresql/data